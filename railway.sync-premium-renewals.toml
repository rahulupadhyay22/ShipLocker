[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python manage.py sync_premium_renewals"
cronSchedule = "15 2 * * *"
restartPolicyType = "NEVER"
